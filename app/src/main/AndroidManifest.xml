<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.samuel.sds_kariyavilai">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SDS_Kariyavilai"
        android:usesCleartextTraffic="true">
        <activity android:name=".Add_Member"
            android:label="Admin Settings"></activity>
        <activity android:name=".AdminSettings"
            android:label="Admin Settings"/>
        <activity
            android:name=".AdminDashBoard"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".Event_Gallery" />
        <activity
            android:name=".Worshiptime"
            android:label="Worship Schedule" />
        <activity
            android:name=".Contacts"
            android:label="Contacts" />
        <activity
            android:name=".Dashboard"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".Login"
            android:label="User Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>